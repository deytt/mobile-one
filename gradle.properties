org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

kotlin.code.style=official
kotlin.mpp.enableCInteropCommonization=true

android.useAndroidX=true
android.nonTransitiveRClass=true

# Fundação KMP: shared usa androidTarget()/com.android.library (padrão AGP 8.x).
# A migração para o plugin com.android.kotlin.multiplatform.library (AGP 9+) fica para quando
# o projeto atualizar o AGP além de 9.0 (ver ADR correspondente antes de migrar).
