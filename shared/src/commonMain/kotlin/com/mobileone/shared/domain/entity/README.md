# domain/entity

Entidades de negócio (ex: `Account`, `Transaction`, `AuthToken`, `PixTransfer`).

Cada entidade é adicionada na PR da spec correspondente (ver `docs/specs/`), seguindo a
convenção de nomenclatura em [`04-kmp-conventions.mdc`](../../../../../../../.cursor/rules/04-kmp-conventions.mdc).
