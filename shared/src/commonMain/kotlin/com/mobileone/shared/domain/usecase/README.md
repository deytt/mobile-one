# domain/usecase

Use cases de negócio (ex: `LoginWithCredentialsUseCase`, `GetTransactionHistoryUseCase`).

Cada use case é adicionado na PR da spec correspondente (ver `docs/specs/`), seguindo a
convenção `Verbo + Substantivo + UseCase` descrita em `.cursor/rules/04-kmp-conventions.mdc`.
